# Core
sqlalchemy>=1.4
schedule>=1.2
pyotp>=2.9
requests>=2.31      # Used by Groww broker (REST API)

# Backtesting (open source, free NSE/BSE data)
yfinance>=0.2.40
pandas>=1.5.0

# Broker SDKs — uncomment ONLY the one you use:
# pip install smartapi-python pyotp   ← Angel One  (recommended)
# pip install kiteconnect             ← Zerodha
# pip install upstox-python-sdk       ← Upstox
# pip install dhanhq                  ← Dhan
# pip install fyers-apiv3             ← Fyers
# (Groww uses requests, already included above)
#
# smartapi-python>=1.3.4
# kiteconnect>=4.2.0
# upstox-python-sdk>=2.0.0
# dhanhq>=2.0.0
# fyers-apiv3>=3.0.0
